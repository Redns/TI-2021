#ifndef __FFT_H
#define __FFT_H

#define PI 3.1415926

typedef double ElemType;

/* 定义复数结构体 */
typedef struct{
    ElemType real;
    ElemType imag;
}complex;


void    ChangeSeat(complex* input);        // 变址
complex mul_complex(complex a, complex b);

#endif
